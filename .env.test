# Environnement des tests unitaires (NODE_ENV=test → .env.local n'est PAS chargé).
# Aucune valeur sensible ici : tout est factice et hors-réseau.
PGLITE_DATA_DIR=:memory:
SESSION_SECRET=test-secret-test-secret-test-secret-42
ADMIN_EMAIL=lea@test.local
ADMIN_PASSWORD=test-password
STRIPE_WEBHOOK_SECRET=whsec_test_secret_for_unit_tests
NEXT_PUBLIC_SITE_URL=http://localhost:3000
# Clé factice : jamais d'appel réseau en test (signatures webhook uniquement)
STRIPE_SECRET_KEY=sk_test_dummy_key_offline
